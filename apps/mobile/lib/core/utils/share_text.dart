String buildChallengeShareText({
  required String challengeTitle,
  required int completedDays,
  required int totalDays,
}) {
  return 'I completed $completedDays of $totalDays days in $challengeTitle on Vital30.';
}
